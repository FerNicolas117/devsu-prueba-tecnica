server:
  port: 8081

spring:
  application:
    name: ms-clientes

  # PostgreSQL
  datasource:
    url: jdbc:postgresql://localhost:5436/db_clientes
    username: devsu
    password: devsu2026
    driver-class-name: org.postgresql.Driver

  # JPA / Hibernate
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        # dialect: org.hibernate.dialect.PostgreSQLDialect

  # RabbitMQ
  rabbitmq:
    host: localhost
    port: 5672
    username: devsu
    password: devsu2026

# Actuator (health checks)
management:
  endpoints:
    web:
      exposure:
        include: health,info